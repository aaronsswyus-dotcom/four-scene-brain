"""branches.3d — V1: robot-job-scene ONLY (full 3D belongs to V4).

target='3d', modality='geometry'. Backbone ALL MOCK in V1
(DreamGaussian/TRELLIS via Azure later, behind adapter.py).

NOTE: package name starts with a digit; import via importlib or the helper in
adapter.py. Demos use: importlib.import_module('branches.3d.adapter').
"""

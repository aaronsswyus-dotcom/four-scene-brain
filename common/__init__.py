"""four-scene-brain common layer (FROZEN kernel).

modality-agnostic orchestration kernel. Scene-agnostic by contract:
- no scene names, no backbone names, no modality if/else
- pure Python stdlib, zero third-party runtime dependencies
- see docs/common-contract.md (v1.0-FROZEN)
"""

__version__ = "1.0.0"
